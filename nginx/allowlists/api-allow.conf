# API allowlist (edit to restrict)
# Examples:
# allow 203.0.113.10;           # single IPv4
# allow 203.0.113.0/24;         # IPv4 CIDR
# allow 2001:db8::/32;          # IPv6 CIDR
# After listing your allowed ranges, ensure 'deny all;' remains in the server config.

# Allowed clients
allow 95.90.236.229;
